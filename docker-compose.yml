services:
  edupredictor:
    build: .
    container_name: edupredictor
    ports:
      - "5000:5000"
    environment:
      FLASK_DEBUG: "0"
      FLASK_USE_RELOADER: "0"
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
